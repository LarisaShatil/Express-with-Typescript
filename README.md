FIRST STEP: 
## Set up an Express+TypeScript environment
1. Initialise Node project with `npm init -y`
2. Install `express` as a dependency
3. Install `typescript` and `@types/express` as **dev dependencies** (npm i -D typescript @types/express)
4. Create and set up tsconfig.json
   a. Install and extend `@tsconfig/node20`
   b. Add compiler options: `rootDir` and `outDir`

## Definitely Typed Repository
Link: https://github.com/DefinitelyTyped/DefinitelyTyped

1. @types/express
2. @types/cors